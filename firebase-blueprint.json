{
  "entities": {
    "CrewMember": {
      "title": "CrewMember",
      "description": "An RSVP attendee representing a pilot registering for Captain Mateo's birthday flights.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Document ID or unique ID for the crew member."
        },
        "name": {
          "type": "string",
          "description": "Full name of the crew member/pilot."
        },
        "status": {
          "type": "string",
          "enum": [
            "confirmed",
            "declined"
          ],
          "description": "Whether the pilot is ready to fly or on reserve."
        },
        "copilots": {
          "type": "number",
          "description": "Number of additional family members or flight copilots registering under this name."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Server or ISO timestamp of the RSVP."
        }
      },
      "required": [
        "id",
        "name",
        "status",
        "copilots",
        "timestamp"
      ]
    },
    "AppConfig": {
      "title": "AppConfig",
      "description": "Global settings such as Mateo's captain photo shared across all users.",
      "type": "object",
      "properties": {
        "photo": {
          "type": "string",
          "description": "Base64 encoded string representing Mateo's photo."
        },
        "photos": {
          "type": "array",
          "description": "Array of Base64 encoded strings representing Mateo's multiple slideshow photos."
        }
      },
      "required": []
    }
  },
  "firestore": {
    "crew/{crewId}": {
      "schema": "CrewMember",
      "description": "Collection of all flight crew RSVPs."
    },
    "config/{configId}": {
      "schema": "AppConfig",
      "description": "Global custom configurations for the invitation."
    }
  }
}
